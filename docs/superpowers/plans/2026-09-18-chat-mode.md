# Official Chat Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a persistent official DeepSeek Chat surface to the Tauri desktop app with a top-level Harness/Chat switch and independent failure/storage boundaries.

**Architecture:** Keep the existing React shell and Harness iframe. Add a Rust-owned Tauri child WebView with a dedicated data directory, explicit navigation policy, and manual bounds below the shell navbar. A React store persists the selected mode and coordinates shell commands/events.

**Tech Stack:** Tauri 2, Rust, React 19, TypeScript, Valtio Define, HeroUI, i18next, Vitest.

## Global Constraints

- Preserve the current Harness iframe and Rust process lifecycle.
- Chat storage must be isolated from Harness storage.
- All user-facing strings must be localized in `en-US.json` and `zh-CN.json`.
- Rust errors use uppercase prefixes and all URL policy decisions happen in Rust.
- Run `pnpm typecheck`, focused Vitest tests, `cargo check`, and focused Rust tests.

---

### Task 1: Add the Rust Chat WebView manager

**Files:**
- Create: `src-tauri/src/desktop/chat.rs`
- Modify: `src-tauri/src/desktop/mod.rs`
- Modify: `src-tauri/src/desktop/builder.rs`
- Modify: `src-tauri/src/lib.rs`
- Modify: `src-tauri/capabilities/default.json`
- Test: `src-tauri/src/desktop/chat.rs`

**Interfaces:**
- Produces `ChatWebviewManager`, `ChatMode`, `ChatStatus`, `CHAT_WEBVIEW_LABEL`, and URL/bounds helpers.
- Rust commands use the `desktop_chat_*` namespace: select, retry, clear, status, layout, and open_browser.

- [x] Define URL allowlist and `ChatStatus` serialization.
- [x] Create the WebView with `WebviewBuilder`, dedicated data directory, external URL, `on_navigation`, `on_new_window`, `on_page_load`, and explicit measured layout.
- [x] Apply `Rect` bounds below the shell navbar and update them on parent resize.
- [x] Implement idempotent show/hide/retry/shutdown and generation checks for stale callbacks.
- [x] Blank and close Chat views, await profile data clearing, then advance the stored profile generation.
- [x] Register commands and chat event emission in the existing Tauri builder.
- [x] Add unit tests for URL policy, bounds, and stale settlement transitions.

### Task 2: Add the React desktop-mode store

**Files:**
- Create: `src/store/modules/desktop-mode/types.ts`
- Create: `src/store/modules/desktop-mode/store.ts`
- Create: `src/store/modules/desktop-mode/index.ts`
- Modify: `src/store/index.ts`
- Test: `test/desktop-mode.test.ts`

**Interfaces:**
- Produces `desktopMode` with `selected`, `chatStatus`, `select`, `retryChat`, and `clearChatData`.
- Consumes Tauri commands and `dsh-chat-status` events.

- [x] Define the persisted mode and status types.
- [x] Load and validate persisted mode, defaulting to Harness.
- [x] Serialize transitions and call Rust commands.
- [x] Subscribe and unsubscribe through the existing Tauri listener helper scoped to the owning shell webview.
- [x] Add tests for restore, ordering, failure, retry, and clear actions.

### Task 3: Add localized navbar switch and Chat status actions

**Files:**
- Modify: `src/layout/components/navbar.tsx`
- Modify: `src/layout/components/webview.tsx`
- Create: `src/layout/components/chat-surface.tsx`
- Modify: `src/i18n/locales/en-US.json`
- Modify: `src/i18n/locales/zh-CN.json`
- Test: `test/desktop-mode-ui.test.tsx`

**Interfaces:**
- Navbar calls `desktopMode.select` and exposes localized `Harness`/`Chat` controls.
- Chat surface reads status and exposes retry, browser fallback, and clear data actions.

- [x] Render the switch without disrupting existing navbar actions or macOS menu behavior.
- [x] Keep Harness content mounted while Chat is selected and hide it through the shell layout.
- [x] Render Chat loading/error overlays in the shell content region.
- [x] Add clear-data confirmation using the existing overlay conventions.
- [x] Add localized labels, descriptions, errors, and accessible names.
- [x] Run native UI smoke scenarios for switching, settings, clear confirmation, fullscreen, and official page loading.

### Task 4: Integrate startup, resizing, and security policies

**Files:**
- Modify: `src/layout/index.tsx`
- Modify: `src/layout/components/webview.tsx`
- Modify: `src-tauri/src/desktop/builder.rs`
- Modify: `src-tauri/tauri.conf.json`
- Modify: `src-tauri/capabilities/default.json`

- [x] Restore the selected mode during startup and send it after the shell is ready.
- [x] Keep Harness startup independent from Chat creation.
- [x] Wire parent resize and window visibility events to the child WebView.
- [x] Give Chat no remote capabilities; allow only the exact official Chat origin in its navigation policy and deny its application commands.
- [x] Route unrelated new windows and redirects through the existing opener bridge.

### Task 5: Verify and document the feature

**Files:**
- Modify: `README.md`
- Modify: `README.en.md`
- Modify: `docs/DEVELOPMENT.md`
- Create: `docs/CHAT_MODE.md`

- [x] Document mode switching, storage isolation, clear-data behavior, and navigation policy.
- [x] Run `pnpm typecheck`.
- [x] Run focused Vitest tests for desktop mode and existing layout tests.
- [x] Run `cargo check` and focused Rust tests.
- [x] Run a desktop smoke test when the platform toolchain is available.
- [x] Review the diff for hardcoded UI strings, unsafe navigation, and generated artifacts.

## Completion evidence

See [Chat mode verification](../../CHAT_MODE_QA.md) for commands, native scenarios, platform limits, and packaging status. The macOS clearing path uses the completion-aware WebKit API because wry 0.55 retains closed WKWebViews.
